/** Initializes a new Sprite game and starts running it. */
class Main {
    function void main() {
        var SpriteHandler game;
        let game = SpriteHandler.new();
        do game.run();
        do game.dispose();
        return;
    }
}
